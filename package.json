{
  "name": "athlix-monorepo",
  "version": "1.0.0",
  "description": "ATHLIX - Startup MVP & Production Foundation for Martial Artists & Combat Sports",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "install-all": "npm install && npm install --prefix client --legacy-peer-deps && npm install --prefix server --legacy-peer-deps",
    "client": "npm run dev --prefix client",
    "server": "npm run dev --prefix server",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build": "npm run build --prefix client",
    "start": "npm start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  }
}
